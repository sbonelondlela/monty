#include "monty.h"
/**
 * f_stack - prints top
 * @head: stacks the head
 * @counter: line_num
 * Return: no return
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
